<ul class="navbar-nav ">
    @if (User.Identity.IsAuthenticated)
    {
        <li class="nav-item">
            <form method="post" class="form-inline" asp-controller="User" asp-action="Logout" >

                <button type="submit" class="btn btn-outline-danger">CERRAR SESIÓN: @User.Identity.Name</button>
            </form>
        </li>
    }
    @*else
    {
        <li class="nav-item">
            <a type="button" class="btn btn-outline-success" asp-controller="Login" asp-action="Logout">INICIAR SESIÓN</a>
        </li>
    }*@
</ul>